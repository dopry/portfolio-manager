/** @type { import('mocha').MochaInstanceOptions } */
module.exports = {
  color: true,
  exit: true,
  extension: ['ts'],
  // spec: ['src/**/*.spec.ts'],
  require: "source-map-support/register",
  // loader: 'ts-node/esm',
}